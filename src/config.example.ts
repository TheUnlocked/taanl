// Obtain your discord token either by using a bot account and going through the normal API or by sniping your own token from the Discord client.
// You'll need to be fast though! Discord likes to `delete localStorage` faster than you can say "`localStorage.token` contains your user auth token"!
export const discordToken = '-----DISCORD TOKEN HERE-----';
// Your slack tokens can be gotten though the normal API.
export const slackTokens = [
    '-----put in a few-----',
    '-----slack tokens-----'
];